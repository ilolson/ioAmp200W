# --- KiCad: per-user state (do NOT commit) ---
*.kicad_prl

# --- Auto-saves & backups ---
*-autosave.kicad_pcb
*-autosave.kicad_sch
*.kicad_pcb-bak
*.kicad_sch-bak
*-bak
*~
backups/

# --- Legacy (v5) artifacts ---
*-cache.lib
fp-info-cache

# --- Generated fabrication/plot outputs ---
# (Gerbers, drills, jobs, reports, positions, zip packs)
*.gbr
*.ger
*.drl
*.d356
*.gbrjob
*.map
*.rpt
*.pos
*.zip

# --- BOM / assembly exports ---
*ibom.html
*ibom.json
*-bom.*
*_bom.*

# --- Netlists (usually regenerated) ---
*.net

# --- SPICE/Simulator outputs ---
*.raw
*.log
*.plt
*.cir.out

# --- Autorouter (e.g., FreeRouting) ---
*.dsn
*.ses

# --- Generic temp files ---
*.tmp
*.swp

# --- STM32CubeIDE / Eclipse workspace cruft (never commit) ---
.metadata/

# If your repo is a multi-project workspace, ignore other projects' workspace bits too
**/.metadata/

# --- Project-local Eclipse noise (keep .project / .cproject / most of .settings) ---
# This one tends to be machine-specific and regenerates frequently:
.settings/language.settings.xml

# Optional: ignore debug launch configs if you don't want to share them
*.launch

# --- Build output folders (auto-generated by toolchain) ---
# CubeIDE typically uses "Debug/" and "Release/" (and variants like Debug_RAM)
**/[Dd]ebug*/
**/[Rr]elease*/
**/Build*/
**/build*/

# --- Compiled objects & intermediates ---
*.o
*.obj
*.d
*.su
*.map
*.lst
*.lss

# --- Binaries / images (reproducible from source) ---
*.elf
*.axf
*.hex
*.bin
*.s19
*.srec
*.out

# --- Coverage / profiling ---
*.gcno
*.gcda
*.gcov
gmon.out

# --- Logs / caches / temps ---
*.log
.cache/
*.tmp

# --- OS/editor miscellany ---
.DS_Store
Thumbs.db
.vscode/
.idea/

/build/
*.uf2
*.elf
*.bin
*.map
.vscode/
.DS_Store
pico_sdk_import.cmake
.env

### Mac OS ###
.DS_Store